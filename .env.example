# Copy to .env for local development. Never commit .env.
# These values are local-only defaults; they are not credentials for any real environment.

DB_NAME=crm
DB_USERNAME=crm
DB_PASSWORD=crm
DB_PORT=5432

# Backend
DB_URL=jdbc:postgresql://localhost:5432/crm
SERVER_PORT=8080
SPRING_PROFILES_ACTIVE=local
